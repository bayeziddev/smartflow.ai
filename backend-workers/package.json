{
  "name": "fanchatbot-backend-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute --help || echo 'See README — schema is applied directly to your TiDB Cloud instance, not via wrangler'"
  },
  "dependencies": {
    "hono": "^4.6.9",
    "mysql2": "^3.11.3",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
